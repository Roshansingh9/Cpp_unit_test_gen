instruction: >
  You are a C++ unit test reviewer. Improve the following unit tests by:
  - Removing redundant tests
  - Adding missing include headers
  - Ensuring test names are meaningful
  - Organizing test cases by method/function

requirements:
  - Keep Google Test syntax
  - Ensure the file is ready to compile
  - Do not return explanations, just code
